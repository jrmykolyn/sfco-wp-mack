( function( window, document ) {
    'use strict';

    // Vendor scripts which bind their primary exports to the `window` object can be imported via `require( ... )`.
    // eg. `require( 'script-name' )`

    // Vendor scripts which return their members via `module.exports` can be added as properties of the `window`.
    // eg. `window.globalIdentifier = require( 'script-name' )`
    window.objectFitImages = require( 'object-fit-images' );

} )( window, document );
